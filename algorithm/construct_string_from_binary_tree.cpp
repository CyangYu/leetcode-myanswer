#include "node.h"
#include "common.h"

USESTD

class Solution {
public:
    string tree2str(TreeNode* t) {
    }

    void preorder(TreeNode *root, string &str) {
        TreeNode *node = root;
        stack<TreeNode *> s;
    }
};